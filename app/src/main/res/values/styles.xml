<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="Asos" parent="@android:style/Widget.TextView">
        <item name="android:textColor">@color/default_text_colour_selector</item>
    </style>
    <style name="Asos.Heavy" parent="@style/Asos">
        <item name="android:fontFamily">@font/futura_pt_reg_heavy</item>
    </style>
    <style name="London" parent="@style/Asos.Heavy">
        <item name="android:textAllCaps">true</item>
    </style>

</resources>
